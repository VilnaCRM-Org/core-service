# Repository-tracked defaults for GitHub Codespaces bootstrap.
# Secrets must stay in Codespaces secrets, not in git.

OPENCODE_NPM_PACKAGE="${OPENCODE_NPM_PACKAGE:-opencode-ai@1.1.53}"
OPENCODE_MODEL="${OPENCODE_MODEL:-openrouter/openai/gpt-5.2-codex}"
OPENCODE_PROVIDER_BASE_URL="${OPENCODE_PROVIDER_BASE_URL:-https://openrouter.ai/api/v1}"
OPENCODE_ENABLED_PROVIDERS="${OPENCODE_ENABLED_PROVIDERS:-openrouter}"
OPENCODE_DEFAULT_AGENT="${OPENCODE_DEFAULT_AGENT:-build}"

GH_HOST="${GH_HOST:-github.com}"
GH_GIT_PROTOCOL="${GH_GIT_PROTOCOL:-ssh}"
GH_PROMPT="${GH_PROMPT:-disabled}"
GH_TOKEN_VAR="${GH_TOKEN_VAR:-GH_AUTOMATION_TOKEN}"

CODESPACE_GITHUB_ORG="${CODESPACE_GITHUB_ORG:-VilnaCRM-Org}"
