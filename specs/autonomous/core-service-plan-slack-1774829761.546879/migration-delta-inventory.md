# Migration Delta Inventory

This control artifact tracks the meaningful documentation and skill-surface deltas for issue `#155`.
Story `1.1` initializes the schema and governance notes; Story `1.2` populates and classifies the meaningful deltas before any broad file edits begin.

## Scope

- Tracked issue bundle: `specs/autonomous/core-service-plan-slack-1774829761.546879/`
- Canonical baseline: current `core-service` guidance remains authoritative unless the target repository disproves it.
- In scope: meaningful routing, policy, taxonomy, example, and portability deltas.
- Out of scope for Story `1.1`: contributor-facing doc edits, skill-file edits, wrapper edits, and runtime code changes.

## Inventory Schema

| Column                | Meaning                                                                                                              |
| --------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Source                | Source file, section, or reference being compared.                                                                   |
| Target                | Target `core-service` file or section affected by the delta.                                                         |
| Delta type            | The kind of difference, such as routing, policy, taxonomy, example, command portability, or reference-only review.   |
| Disposition           | `adopt`, `adapt`, `defer`, or `exclude` once Story `1.2` classifies the delta.                                       |
| Rationale             | Why the disposition is correct for `core-service`.                                                                   |
| Validation needed     | Evidence needed before the delta can be considered done.                                                             |
| Implementation status | Current execution state such as `not started`, `in progress`, `implemented`, `validated`, `deferred`, or `excluded`. |

## Disposition Values

| Value     | Meaning                                                                                                                       |
| --------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `adopt`   | The content is already correct for `core-service` and can be used without a material semantic rewrite.                        |
| `adapt`   | The intent is useful, but commands, examples, paths, stack assumptions, or taxonomy wording must be rewritten.                |
| `defer`   | The item is acknowledged but moved to a later focused issue because it would broaden scope or needs a larger design decision. |
| `exclude` | The content is invalid, redundant, unsupported, or harmful for `core-service`.                                                |

## Read-only References

These references may inform classification work, but they are not edit targets for this issue.

| Path                | Role                                                             | Edit target |
| ------------------- | ---------------------------------------------------------------- | ----------- |
| `_bmad/COMMANDS.md` | Read-only BMALPH command reference for portability checks.       | `no`        |
| `_bmad/config.yaml` | Read-only BMALPH configuration reference for portability checks. | `no`        |

## Delta Inventory

| Source                                                                                                                                                             | Target                                                                                                                                                                                                                   | Delta type                                      | Disposition | Rationale                                                                                                                                                                                                             | Validation needed                                                                                                                                                                 | Implementation status |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| `user-service` top-level skill inventory compared against the existing `core-service` catalog                                                                      | `.claude/skills/` directory tree                                                                                                                                                                                         | `directory parity / catalog shape`              | `exclude`   | `core-service` already contains the shared top-level skill directories plus target-only additions, so no directory import or new top-level skill creation is needed.                                                  | Confirm no new top-level skill directory is introduced as part of this migration.                                                                                                 | `excluded`            |
| `user-service` canonical routing wording that changes issue scope, source-of-truth ownership, or the default route for docs, review, and autonomous planning tasks | `AGENTS.md`, `.claude/skills/AI-AGENT-GUIDE.md`, `.claude/skills/SKILL-DECISION-GUIDE.md`, `.claude/skills/README.md`                                                                                                    | `routing and policy portability`                | `adapt`     | The intent to tighten routing is useful, but `core-service` must remain authoritative for BMALPH usage, autonomous planning, MongoDB correctness, onboarding, and supported commands.                                 | Verify the four canonical docs agree on routing, issue `#155` remains documentation-only, and the default routes for documentation, review, and autonomous planning stay aligned. | `implemented`         |
| `user-service` canonical guidance that conflicts with the existing `core-service` BMALPH, autonomous-planning, MongoDB, or onboarding baseline                     | `AGENTS.md`, `.claude/skills/AI-AGENT-GUIDE.md`, `.claude/skills/SKILL-DECISION-GUIDE.md`, `.claude/skills/README.md`, `README.md`, `docs/getting-started.md`, `docs/onboarding.md`                                      | `source-of-truth conflict`                      | `exclude`   | The architecture freezes `core-service` as the source of truth for these topics, so conflicting source text is not portable.                                                                                          | Confirm later edits preserve the current `core-service` baseline and treat source conflicts as rejected inputs rather than copied text.                                           | `excluded`            |
| `user-service` command reference `make ai-review-loop`                                                                                                             | `AGENTS.md`, `.claude/skills/code-review/SKILL.md`, and any later canonical-doc or mirror-doc review guidance                                                                                                            | `unsupported command portability`               | `exclude`   | The direct command is unavailable in `core-service`; only the underlying review intent may survive through separately adapted guidance that uses the existing `make pr-comments` plus `make ci` workflow.             | Confirm `make ai-review-loop` does not appear in edited files and that review instructions use real `core-service` commands.                                                      | `excluded`            |
| `user-service` command reference `make schemathesis-validate`                                                                                                      | Canonical routing docs, repo-facing mirrors, and any migrated validation guidance                                                                                                                                        | `unsupported command portability`               | `exclude`   | The command is not exposed by the `core-service` `Makefile`, and the planning bundle only requires adaptation when an equivalent target workflow exists.                                                              | Confirm no edited file introduces `make schemathesis-validate`; use existing validation entrypoints instead.                                                                      | `excluded`            |
| `user-service` command reference `make openapi-diff` in OpenAPI-validation guidance                                                                                | `.claude/skills/openapi-development/**`, `.claude/skills/README.md`, and repo-facing mirror docs such as `docs/onboarding.md`                                                                                            | `unsupported command portability`               | `exclude`   | The command is not exposed by the `core-service` `Makefile`, so direct portability would create broken workflow guidance even where the source intent is valuable.                                                    | Confirm no edited file treats `make openapi-diff` as a supported `core-service` command; use only real validation entrypoints.                                                    | `excluded`            |
| `user-service` mandatory new-feature verification gate that requires running every skill after each new feature                                                    | `AGENTS.md` and `.claude/skills/AI-AGENT-GUIDE.md`                                                                                                                                                                       | `policy portability / governance`               | `defer`     | The stricter governance intent is meaningful, but adopting it would change repository-wide completion policy and needs a separate decision beyond this migration’s routing-focused scope.                             | Record the delta now and ensure issue `#155` does not silently import the all-skills gate without a follow-up policy decision.                                                    | `deferred`            |
| `user-service` locked-configuration exception policy around `make validate-configuration` and protected files                                                      | `AGENTS.md`, `.claude/skills/AI-AGENT-GUIDE.md`, and any later CI-governance guidance                                                                                                                                    | `policy portability / configuration governance` | `adapt`     | `core-service` exposes `make validate-configuration`, so the policy intent may be portable, but the protected-file list and escalation wording must be rewritten for the target repository instead of copied blindly. | Confirm any later adoption references the real `core-service` command and protected files, and does not import source-only exception handling verbatim.                           | `not started`         |
| `user-service` review-policy text that is stricter or more operationally prescriptive than current `core-service` review guidance                                  | `.claude/skills/code-review/SKILL.md`                                                                                                                                                                                    | `workflow policy adaptation`                    | `adapt`     | Useful review rigor may be adopted, but it must be rewritten around the target repo’s supported review flow and quality gates.                                                                                        | Confirm the review skill still routes architecture, organization, and quality concerns to the correct companion skills while using only supported commands.                       | `implemented`         |
| `user-service` review-style organization example that no longer lives under the same ownership in `core-service`                                                   | `.claude/skills/code-organization/examples/**` and `.claude/skills/code-review/examples/organization-fixes.md`                                                                                                           | `example ownership and portability`             | `adapt`     | The example intent is relevant, but ownership has shifted in `core-service` and must be reconciled through the owning skill or cross-reference rather than duplicated.                                                | Confirm later example guidance makes ownership explicit and uses `core-service` paths, commands, and bounded-context terminology.                                                 | `implemented`         |
| `user-service` code-organization naming examples that hard-code source-stack technology such as `MySQLCustomerRepository.php`                                      | `.claude/skills/code-organization/SKILL.md` and related organization examples                                                                                                                                            | `wrong-stack example portability`               | `adapt`     | The placement and naming guidance is useful, but stack-specific examples must be rewritten to use `core-service` technology and naming conventions rather than the source repo’s storage vocabulary.                  | Confirm organization guidance uses `core-service` paths and technology examples and does not preserve MySQL-specific repository naming.                                           | `not started`         |
| `user-service` documentation-creation example and template content with source-only database, auth, or bounded-context references                                  | `.claude/skills/documentation-creation/examples/**` and `.claude/skills/documentation-creation/reference/doc-templates.md`                                                                                               | `repo-specific example portability`             | `exclude`   | The architecture forbids verbatim copy of repo-specific examples; only genuinely portable intent may survive after rewrite, and the current source examples are tied to user-service database/auth contexts.          | Confirm no repo-specific `user-service` documentation example or template is copied verbatim into `core-service`.                                                                 | `excluded`            |
| OpenAPI-related routing overlap discovered while comparing skill catalogs and guidance surfaces                                                                    | `AGENTS.md`, `.claude/skills/AI-AGENT-GUIDE.md`, `.claude/skills/SKILL-DECISION-GUIDE.md`, `.claude/skills/README.md`, `.claude/skills/developing-openapi-specs/SKILL.md`, `.claude/skills/openapi-development/SKILL.md` | `taxonomy clarification`                        | `adapt`     | The target repo keeps both skills, but their boundary must be rewritten explicitly so `developing-openapi-specs` is the default path and `openapi-development` is the advanced path.                                  | Confirm every affected discovery surface states the same default-versus-advanced split without implying duplicate defaults.                                                       | `implemented`         |
| Broader consolidation of the two OpenAPI skills into one primary skill                                                                                             | OpenAPI-related canonical docs and skill-local docs                                                                                                                                                                      | `taxonomy simplification follow-up`             | `defer`     | The PRD and architecture treat consolidation as a separate follow-up decision so this issue can stay focused on clarification instead of catalog redesign.                                                            | Record the clarification now and leave any consolidation work to a later issue.                                                                                                   | `deferred`            |
| `user-service` MySQL-oriented performance-analysis guidance                                                                                                        | `.claude/skills/query-performance-analysis/**` and any comparison-driven performance references                                                                                                                          | `wrong-stack guidance`                          | `exclude`   | `core-service` is MongoDB-based and already ships MongoDB-oriented profiler guidance; MySQL-specific advice is not portable.                                                                                          | Confirm no MySQL slow-query guidance is introduced during migration and existing MongoDB guidance remains authoritative.                                                          | `excluded`            |
| `user-service` OAuth-specific guidance that appears in compared policy, example, or operational text                                                               | Any in-scope canonical doc, skill module, or repo-facing mirror where the source text would otherwise be migrated                                                                                                        | `wrong-context guidance`                        | `exclude`   | OAuth-specific assumptions are not automatically portable to `core-service` and are explicitly called out by the planning bundle as adapt-or-exclude material.                                                        | Confirm no edited file imports OAuth-specific assumptions unless a later issue deliberately expands scope.                                                                        | `excluded`            |
| Wrapper wording deltas implied by later canonical-routing edits                                                                                                    | `.agents/skills/**/SKILL.md` wrappers, especially BMALPH-facing wrappers                                                                                                                                                 | `wrapper mirror policy`                         | `adapt`     | Wrappers may mirror settled canonical wording, but they must not invent routing policy or change unless canonical language actually moved.                                                                            | Confirm later wrapper edits, if any, only mirror canonical routing changes and unchanged wrappers are recorded with rationale.                                                    | `validated`           |
| `user-service` onboarding or repo-facing mirror text that could improve contributor flow without changing the `core-service` baseline                              | `README.md`, `docs/getting-started.md`, `docs/onboarding.md`                                                                                                                                                             | `mirror-doc portability`                        | `adapt`     | The mirror docs must reflect the final canonical routing model, but the existing `core-service` BMALPH and onboarding flow remain authoritative.                                                                      | Confirm mirror docs do not contradict canonical routing docs and do not introduce unsupported commands or wrong-stack assumptions.                                                | `implemented`         |
| Potential terminology drift reaching the runtime reference anchors during migration                                                                                | `docs/design-and-architecture.md`, `docs/developer-guide.md`                                                                                                                                                             | `reference-anchor evaluation`                   | `defer`     | These files should remain unchanged unless later routing or skill edits would otherwise make them inaccurate, so the decision is deferred until the rest of the migration settles.                                    | Explicitly evaluate both files later and record either `unchanged` with rationale or the minimal necessary correction.                                                            | `validated`           |
| Wording-only differences between `user-service` and `core-service` that do not change routing, policy, commands, examples, or correctness                          | All in-scope comparison surfaces                                                                                                                                                                                         | `low-signal wording churn`                      | `exclude`   | The planning bundle explicitly rejects low-value churn so the migration stays selective and reviewable.                                                                                                               | Confirm inventory and later edits ignore wording-only drift unless it changes meaning.                                                                                            | `excluded`            |
